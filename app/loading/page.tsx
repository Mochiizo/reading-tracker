import LoadingScreen from '@/components/LoadingScreen';

/**
 * Page de chargement de l'application
 * Affiche un écran de chargement pendant le traitement des données utilisateur
 * Utilisée principalement après la connexion pour charger les données initiales
 */
export default function LoadingPage() {
  return <LoadingScreen />;
} 